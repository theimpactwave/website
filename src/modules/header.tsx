import ComponentHeader from "@/components/header";
import Hero from "@/components/hero";

const Header = () => {
  return (
    <>
      <ComponentHeader>
        <Hero />
      </ComponentHeader>
    </>
  );
};

export default Header;
